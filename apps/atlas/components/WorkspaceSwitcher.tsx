"use client";

import { useEffect, useMemo, useRef, useState } from "react";

export type WorkspaceStatus = "Published" | "Review" | "Needs review" | "Draft" | "Processing";
export type WorkspaceSwitcherItem = { id: string; name: string; status: WorkspaceStatus; prdCount: number; base?: string; createdBy: string; createdAt: string; modifiedBy: string; modifiedAt: string; relative: string; displayedHead?: string; executionProvenance?: string; unavailableReason?: string };
export type WorkspaceSwitcherModel = { projectName: string; selectedId: string; workspaces: WorkspaceSwitcherItem[] };

const initials = (name: string) => name === "Atlas" ? "AT" : name.split(/\s+/).map((part) => part[0]).join("").slice(0, 2).toUpperCase();
const statusClass = (status: WorkspaceStatus) => status.toLowerCase().replace(/\s+/g, "-");
const dateValue = (value: string) => { const parsed = Date.parse(value); return Number.isNaN(parsed) ? "" : new Date(parsed).toISOString().slice(0, 10); };

export function WorkspaceSwitcher({ model, onNewWorkspace, onSelect }: { model: WorkspaceSwitcherModel; onNewWorkspace?: () => void; onSelect: (id: string) => void }) {
  const [open, setOpen] = useState(false); const [query, setQuery] = useState(""); const [status, setStatus] = useState<WorkspaceStatus | "">(""); const [users, setUsers] = useState<string[]>([]); const [createdAfter, setCreatedAfter] = useState(""); const [createdBefore, setCreatedBefore] = useState(""); const [modifiedAfter, setModifiedAfter] = useState(""); const [modifiedBefore, setModifiedBefore] = useState(""); const [filterOpen, setFilterOpen] = useState<"users" | "created" | "modified" | "">(""); const [notice, setNotice] = useState("");
  const root = useRef<HTMLDivElement>(null); const trigger = useRef<HTMLButtonElement>(null); const search = useRef<HTMLInputElement>(null); const selected = model.workspaces.find((item) => item.id === model.selectedId) ?? model.workspaces[0];
  const people = useMemo(() => [...new Set(model.workspaces.flatMap((item) => [item.createdBy, item.modifiedBy]))].sort(), [model.workspaces]);
  const activeFilterCount = (status ? 1 : 0) + users.length + [createdAfter, createdBefore, modifiedAfter, modifiedBefore].filter(Boolean).length;
  const results = useMemo(() => model.workspaces.filter((item) => { const created = dateValue(item.createdAt); const modified = dateValue(item.modifiedAt); const matchesUser = !users.length || users.includes(item.createdBy) || users.includes(item.modifiedBy); const matchesDates = (!createdAfter || created >= createdAfter) && (!createdBefore || created <= createdBefore) && (!modifiedAfter || modified >= modifiedAfter) && (!modifiedBefore || modified <= modifiedBefore); return (!status || item.status === status) && matchesUser && matchesDates && `${item.name} ${item.status} ${item.base ?? ""} ${item.createdBy} ${item.modifiedBy} ${item.displayedHead ?? ""} ${item.executionProvenance ?? ""}`.toLowerCase().includes(query.trim().toLowerCase()); }), [model.workspaces, query, status, users, createdAfter, createdBefore, modifiedAfter, modifiedBefore]);
  const published = results.filter((item) => item.status === "Published"); const active = results.filter((item) => item.id === model.selectedId && item.status !== "Published"); const other = results.filter((item) => item.status !== "Published" && item.id !== model.selectedId);
  const clearFilters = () => { setStatus(""); setUsers([]); setCreatedAfter(""); setCreatedBefore(""); setModifiedAfter(""); setModifiedBefore(""); setFilterOpen(""); setNotice("Workspace filters cleared."); };
  useEffect(() => { if (!open) return; const timeout = window.setTimeout(() => search.current?.focus(), 0); const outside = (event: PointerEvent) => { if (!root.current?.contains(event.target as Node)) { setOpen(false); setFilterOpen(""); } }; const escape = (event: KeyboardEvent) => { if (event.key === "Escape") { setOpen(false); setFilterOpen(""); trigger.current?.focus(); } }; document.addEventListener("pointerdown", outside); document.addEventListener("keydown", escape); return () => { window.clearTimeout(timeout); document.removeEventListener("pointerdown", outside); document.removeEventListener("keydown", escape); }; }, [open]);
  if (!selected) return null;
  const select = (item: WorkspaceSwitcherItem) => { if (item.unavailableReason) { setNotice(`${item.name}: ${item.unavailableReason}`); return; } onSelect(item.id); setOpen(false); setNotice(`${item.name} is now the selected workspace context.`); trigger.current?.focus(); };
  const toggleUser = (person: string) => setUsers((current) => current.includes(person) ? current.filter((item) => item !== person) : [...current, person]);
  const AuditContext = ({ item }: { item: WorkspaceSwitcherItem }) => item.displayedHead || item.executionProvenance ? <div className="workspace-provenance"><span>{item.displayedHead && <>HEAD <b>{item.displayedHead}</b></>}</span>{item.executionProvenance && <span>Run <b>{item.executionProvenance}</b></span>}</div> : null;
  const Row = ({ item }: { item: WorkspaceSwitcherItem }) => <button aria-current={item.id === model.selectedId ? "true" : undefined} aria-describedby={item.unavailableReason ? `reason-${item.id}` : undefined} className={`workspace-audit-row ${item.id === model.selectedId ? "current" : ""}`} onClick={() => select(item)} type="button"><div className="row-main"><div className="row-title"><strong>{item.name}</strong><span className={`workspace-status ${statusClass(item.status)}`}>{item.status}</span></div>{item.base && <div className="base-meta"><span>Base</span><b>{item.base}</b></div>}<div className="audit-grid"><Audit label="Created" name={item.createdBy} time={item.createdAt} /><Audit label="Last modified" name={item.modifiedBy} time={item.modifiedAt} /></div><AuditContext item={item} />{item.unavailableReason && <span className="workspace-unavailable" id={`reason-${item.id}`}>Unavailable · {item.unavailableReason}</span>}</div><div className="row-side"><span>{item.prdCount} PRD{item.prdCount === 1 ? "" : "s"}</span><small>{item.relative}</small></div></button>;
  const filterMenu = (name: "users" | "created" | "modified", label: string, content: React.ReactNode) => <div className="workspace-filter"><button aria-expanded={filterOpen === name} className={filterOpen === name ? "is-active" : ""} onClick={() => setFilterOpen((current) => current === name ? "" : name)} type="button">{label}{name === "users" && users.length ? <b>{users.length}</b> : null}<span aria-hidden="true">⌄</span></button>{filterOpen === name && <div className="workspace-filter-menu" role="group" aria-label={`${label} filter`}>{content}</div>}</div>;
  return <div className="workspace-switcher" ref={root}><p className="switch-label">Workspace</p><button aria-controls="workspaceMenu" aria-expanded={open} className="workspace-trigger" onClick={() => setOpen((value) => !value)} ref={trigger} type="button"><span className="branch-glyph" aria-hidden="true">⌘</span><span><strong>{selected.name}</strong><small><em className={`workspace-status ${statusClass(selected.status)}`}>{selected.status}</em>modified {selected.relative}</small>{selected.displayedHead && <small className="trigger-head">HEAD {selected.displayedHead}{selected.executionProvenance ? ` · ${selected.executionProvenance}` : ""}</small>}</span><b aria-hidden="true">⌄</b></button><p aria-live="polite" className="sr-only">{notice}</p>{open && <section aria-label="Workspace switcher" className="workspace-menu" id="workspaceMenu"><div className="switcher-head"><div className="switcher-title"><strong>Switch workspace</strong><span>{model.projectName}</span><button aria-label="Close workspace switcher" onClick={() => { setOpen(false); trigger.current?.focus(); }} type="button">×</button></div><label className="workspace-search"><span aria-hidden="true">⌕</span><input onChange={(event) => setQuery(event.target.value)} placeholder="Search workspace, creator, modifier, HEAD, or run…" ref={search} type="search" value={query} /></label><div className="filterbar">{filterMenu("users", "Users", <><div className="filter-menu-title"><span>Created by or modified by</span>{users.length ? <button onClick={() => setUsers([])} type="button">Clear</button> : null}</div>{people.map((person) => <label className="filter-check" key={person}><input checked={users.includes(person)} onChange={() => toggleUser(person)} type="checkbox" />{person}</label>)}</>)}<label className="status-filter">Status<select aria-label="Filter by status" onChange={(event) => setStatus(event.target.value as WorkspaceStatus | "")} value={status}><option value="">All</option>{["Published", "Review", "Needs review", "Draft", "Processing"].map((value) => <option key={value}>{value}</option>)}</select></label>{filterMenu("created", "Created date", <DateFilter after={createdAfter} before={createdBefore} label="Created" setAfter={setCreatedAfter} setBefore={setCreatedBefore} />)}{filterMenu("modified", "Modified date", <DateFilter after={modifiedAfter} before={modifiedBefore} label="Modified" setAfter={setModifiedAfter} setBefore={setModifiedBefore} />)}{activeFilterCount ? <button className="clear-workspace-filters" onClick={clearFilters} type="button">Clear filters <b>{activeFilterCount}</b></button> : null}</div><p className="filter-results" aria-live="polite">{results.length} of {model.workspaces.length} workspaces</p></div><div className="switcher-body">{!results.length && <p className="empty-results">No workspaces match these filters.</p>}<Group title="Published truth" items={published} Row={Row} /><Group title="Active workspace" items={active} Row={Row} /><Group title="Other workspaces" items={other} Row={Row} /></div><footer className="switcher-foot"><span>Workspace identity and audit metadata</span><button onClick={onNewWorkspace} type="button">+ New workspace</button></footer></section>}</div>;
}

function DateFilter({ after, before, label, setAfter, setBefore }: { after: string; before: string; label: string; setAfter: (value: string) => void; setBefore: (value: string) => void }) { return <div className="date-filter"><label>{label} after<input aria-label={`${label} after`} onChange={(event) => setAfter(event.target.value)} type="date" value={after} /></label><label>{label} before<input aria-label={`${label} before`} onChange={(event) => setBefore(event.target.value)} type="date" value={before} /></label></div>; }
function Audit({ label, name, time }: { label: string; name: string; time: string }) { return <div className="audit-block"><span>{label}</span><div><i>{initials(name)}</i><p><b>{name}</b><small>{time}</small></p></div></div>; }
function Group({ title, items, Row }: { title: string; items: WorkspaceSwitcherItem[]; Row: ({ item }: { item: WorkspaceSwitcherItem }) => React.JSX.Element }) { return items.length ? <section className="workspace-group"><h3>{title}</h3>{items.map((item) => <Row item={item} key={item.id} />)}</section> : null; }
