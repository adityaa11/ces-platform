{
  "name": "@atlas/fixtures",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "pnpm --filter @atlas/app run fixtures:generate",
    "test": "pnpm run generate && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
