# CES-GF-AGB-001 — Agents Bridge: Service Boundary and Contracts

**Phase:** 3D — Central Agent Provider Foundation  
**Parent:** Greenfield Product Suite  
**Status:** Implemented locally; hosted CI pending

**Evidence:** [`evidence/CES-GF-AGB-001-local-contracts.md`](evidence/CES-GF-AGB-001-local-contracts.md)

## Goal

Define the contracts and trust boundaries for a centralized, contract-driven,
agent-agnostic and provider-agnostic execution gateway without turning it into
an unrestricted model proxy.

## Work

- Record the service boundary, trust zones, callers, secrets, and data flows.
- Define a versioned, mode-specific structured-generation agent contract plus
  provider, model-alias, execution-context, execution-policy, and
  provider-neutral request contracts.
- Define explicit agent, provider, and model registries that reject invalid,
  duplicate, or unresolved definitions during startup.
- Reserve `tool-assisted`, `remote-agent`, and `deterministic` identifiers as
  extension points without giving them the structured-generation lifecycle;
  implement only `structured-generation` initially.
- Define a versioned tool contract and allowlist policy without implementing
  tools for the first Atlas agent.
- Define authentication identities, per-client agent authorization, quotas,
  and audit identity.
- Define `POST /v1/agents/:agentId/execute` for registered agents and
  compatibility-route delegation rules for `POST /v1/atlas/analyze`.
- Declare the generic route canonical and allow a compatibility route only for
  a proven existing client contract.
- Define common sanitized error, version negotiation, completion, execution
  budget, and bounded diagnostic rules.
- Define the rule that agents own workflow schemas, prompts, transformation,
  and human-review policy while providers own transport translation,
  completion parsing, retries, and usage extraction.
- Update the fail-closed workspace architecture assignment for the new
  application and only its proven dependencies.
- Prohibit arbitrary prompts, response schemas, credentials, provider URLs,
  physical models, and tools in caller input.
- Define exact canonical comparison keys, absent-location behavior, semantic
  hash tie-breakers, run-local ID guarantees, and reference remapping for every
  Atlas result collection.
- Define service execution ceilings for request bytes, source count, aggregate
  and per-source characters, provider response bytes, output tokens, and
  provider attempts.

## Acceptance criteria

- [x] A new agent can be registered without changing shared execution logic.
- [x] A new provider can be registered without changing agent contracts.
- [x] A new execution mode can be introduced without changing the meaning of
      the structured-generation agent contract.
- [x] Duplicate or unresolved agent, provider, and model registrations fail
      during startup.
- [x] Agent definitions cannot access provider credentials and provider
      adapters cannot import Atlas schemas.
- [x] Atlas requires no incompatible CLI or provider-contract change.
- [x] The design cannot accept arbitrary provider URLs, API versions, models,
      prompts, or response contracts from callers.
- [x] Trusted metadata is assigned by bridge code, not generated by Gemini.
- [x] IDs and cross-references have a deterministic normalization algorithm.
- [x] Human review remains mandatory after successful extraction.
- [x] Callers are authorized for specific registered agents and cannot submit
      arbitrary execution configuration.
- [x] Canonical ordering and run-local identifier behavior are implementable
      without an unspecified comparison or semantic-equivalence judgment.

## Required evidence

- [x] Trust-boundary and data-flow document.
- [x] Agent, provider, model, policy, context, and error contract fixtures.
- [x] Registry duplicate, unresolved-reference, and version fixtures.
- [x] Architecture dependency assignment and boundary test.
- [x] Trust-boundary, compatibility-route, and authorization examples.
- [x] Golden canonical-ordering, tie-breaker, missing-location, and
      reference-remapping fixtures.

## Out of scope

- Network server implementation.
- Agent or provider implementations.
- Production deployment.
- Non-Atlas workflow endpoints.

## Depends on

- `CES-GF-ATLAS-005`
