# CES-017 Evidence — Final output and mounted Docker contract

## Independent Stage A

- [Successful core resolution](CES-017-stage-a-success/policy-manifest.json) — exit 0
- [Blocked core resolution](CES-017-stage-a-blocked/policy-manifest.json) — exit 3
- [Conflicting core resolution](CES-017-stage-a-conflict/policy-manifest.json) — exit 4

Each directory contains exactly the normalized `requirement-package.json` and `policy-manifest.json`. CLI tests also compile equivalent YAML and JSON inputs and compare both Stage A files byte for byte.

## Mounted compilation

- [Docker transcript and complete listing](CES-017-docker-output.txt)
- [Mounted core artifacts](CES-017-mounted/core/policy-manifest.json)
- [Mounted Laravel artifacts](CES-017-mounted/adapters/laravel/implementation-plan.json)

The rebuilt image compiled through the host mount without a separate adapter argument. All six expected files are host-writable, the adapter records the project pin `laravel@0.1.0`, core and adapter compilation identities match, and no container-only absolute paths occur.

Repository CI runs `scripts/validate-mounted-output.mjs` after its mounted compile. The validator checks every artifact, writability, identity, adapter pin, and path hygiene. A repository test proves an empty/missing-artifact directory returns exit 1.

`corepack pnpm check` passes all typechecks, 119 tests across 19 test files, and every workspace build.
