# CES-005 Evidence — Independent Core CLI

## Verification result

- `corepack pnpm check` completed successfully on 2026-07-22.
- The compiled entry point runs directly with `node apps/cli/dist/index.js`.
- All 10 test files passed, covering 53 tests.
- Type checking and the production build passed for all implementation projects.

## Attached evidence

- [Compiled CLI help output](CES-005-help.txt)
- [CES-004 successful Policy Manifest](CES-004-policy-manifest.json)
- [CES-004 blocked and conflict diagnostics](CES-004-diagnostics.json)

## Acceptance evidence

The CLI integration suite exercises a JSON requirement and YAML combined project file. It validates both inputs, writes normalized JSON, and resolves a Policy Manifest while the configured adapter ID is deliberately unavailable. This proves core resolution neither imports nor loads a production adapter.

Invalid input tests assert exit code 2, the input filename, and the failing schema field, with no stack trace. Blocked lifecycle and incompatible-parameter runs assert exit codes 3 and 4 respectively, then read each diagnostic manifest back from disk before checking its blocked or conflicting obligation.

The help artifact documents every Phase 1 core command, input/output flags, supported JSON/YAML formats, and exit codes 0, 2, 3, and 4.
