# CES-007 Evidence — Framework-neutral test-fixture adapter

## Classification and selection

- [Versioned fixture metadata](CES-007-fixture-metadata.json)
- [Production/test-mode selection results](CES-007-selection.txt)
- [Package safety documentation](../../../adapters/test-fixture/README.md)

Both package metadata and documentation state that this adapter is test-only, unsafe and unapproved for production guidance, and distinct from the future `generic-guidance` adapter. The SDK registry rejects it unless `test_mode: true` is explicit.

## Generated output

- [Fixture Implementation Package](CES-007-implementation-package.json)
- Source Policy Manifest compilation ID: `sha256:240139824b60a83ac65a17fcbf2db171c604f0fd9bc924f3a24402dfe3a545fa`

Every initial obligation has generic implementation, test, and verification guidance with policy ID, mapping ID, and mapping version provenance. Tests compile the same manifest with reversed obligation order and assert byte-equivalent objects. They also compare the source manifest before and after compilation to prove it is unchanged.

## Contract and gap coverage

All 12 workspace test files pass, covering 67 tests, with successful type checking and production builds.

The fixture tests cover explicit test-mode selection, all nine mappings, source immutability, deterministic ordering, neutral output, and a deliberate unsupported `ATOMIC_RESOURCE_REPLACEMENT` mapping. The gap returns exit code 5 and an adapter report without an Implementation Package.

The full workspace suite runs without a production adapter package. A serialized-output scan rejects production framework and package terminology.

## Cross-adapter evidence

The CES-008 portability test passes the same Policy Manifest object to both adapters. Both generated packages record source compilation ID `sha256:240139824b60a83ac65a17fcbf2db171c604f0fd9bc924f3a24402dfe3a545fa`, and the test proves the source object is byte-unchanged after both compilations.
