# CES-012 Evidence — Examples, documentation, and local delivery

## Local workflows

- [Local setup and CLI transcript](CES-012-local-transcript.txt)
- [Laravel generated output](CES-012-laravel/implementation-task.md)
- [Fixture generated output](CES-012-fixture/implementation-task.md)
- [Blocked diagnostic Policy Manifest](CES-012-blocked/policy-manifest.json)

The successful examples exit 0 and generate all six expected artifacts. Their Policy Manifests are byte-identical. The lifecycle variant exits 3 after writing only the normalized Requirement Package and blocked Policy Manifest.

## Documentation

The [Phase 1 guide](../../../docs/phase-1.md) documents local Node and Docker commands, both compilation stages, verification, all exit codes, boundaries, gaps, and semantic-verification limits. Its extension section explains how to add capabilities/traits, policies, and adapters while preserving the core boundary.

The guide labels the implemented Phase 1 structured-input flow separately from Phase 3 PRD extraction, Phase 4 composable adapters and generic production guidance, and Phase 5 overrides/governance. Documentation scans reject a requirement-to-Laravel core pipeline and distinguish the fixture from approved production guidance.

## Repository CI and Docker

`.github/workflows/test.yml` is repository-only: it has no `workflow_call`, runs the pinned install and full check, builds `ces-cli:local`, and smoke-tests its help command. The multi-stage Dockerfile pins Node 24.12.0 and pnpm 11.15.1.

[Docker validation output](CES-012-docker-output.txt) records the current external blocker: this machine has no running Docker daemon. A real successful build and mounted-workspace run are still required before CES-012 can be marked Done.

## Scope review

Phase 1 contains no natural-language/PDF PRD extraction, overrides, exception governance, adapter component composition, support-level resolver, dependency solver, generic production fallback, dynamic downloading, marketplace, scaffolding, or approval workflow. References to these concepts are documentation of deferred scope, not implementations.
