# CES-009 Evidence — Implementation compiler

## Generated directories

- [Laravel compilation](CES-009-laravel/implementation-task.md)
- [Fixture compilation](CES-009-fixture/implementation-task.md)
- [Blocked two-stage compilation](CES-009-blocked/policy-manifest.json)
- [Adapter-gap report](CES-009-gap/adapter-report.json)

Both successful directories contain `requirement-package.json`, `policy-manifest.json`, `implementation-plan.json`, `implementation-task.md`, `test-manifest.json`, and `verification-manifest.json` generated by the compiled Node.js CLI.

## Shared source identity

The Laravel and fixture `policy-manifest.json` files are byte-identical. Their file SHA-256 is `CB43DFCD91185795D35E5F508F0212D52E190CD0DB4F59781B3C161A3E7EF17A`; both contain compilation ID `sha256:c39bf3df838631b6cd3a645af16d29e514ee86bf6b1c94e793d4d74c14cd082d`.

Every adapter-derived JSON header records this compilation ID plus adapter ID, adapter version, and mapping version. Laravel and fixture guidance differs according to their mappings while validating against the same Adapter SDK schemas.

## Determinism and schema validation

The full workspace check passes 15 test files and 81 tests, plus type checking and production builds for all implementation projects.

Integration tests compile identical inputs into two independent directories and compare every file byte-for-byte. Compiler tests validate each plan, test manifest, and verification manifest through the shared schemas; Markdown generation uses fixed sections, LF endings, stable ordering, and a terminal newline.

## Failure boundaries

The blocked lifecycle example returns exit code 3 after writing only the normalized Requirement Package and diagnostic Policy Manifest. It does not load the deliberately missing adapter used by the integration test and emits no adapter artifacts.

The gap example returns exit code 5 and writes only `adapter-report.json`. No implementation plan, task, test manifest, or verification manifest is present.

## Consumer neutrality

The canonical Markdown addresses the implementer without coding-agent selection, proprietary agent metadata, or vendor-specific invocation commands. Automated scans reject `Codex`, `Claude`, `OpenAI`, and `Anthropic` in generated tasks. Adapter-specific implementation vocabulary appears only because it is selected mapping guidance, not because the artifact targets a particular human or coding agent.
