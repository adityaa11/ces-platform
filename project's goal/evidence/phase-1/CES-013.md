# CES-013 Evidence — Concrete policy parameter propagation

## Generated artifacts

- [Laravel Policy Manifest and derived artifacts](CES-013-laravel/implementation-task.md)
- [Fixture Policy Manifest and derived artifacts](CES-013-fixture/implementation-task.md)
- [Deterministic conflict Policy Manifest](CES-013-conflict-policy-manifest.json)
- [Conflicting source requirement](CES-013-conflict-requirement.yaml)

Both successful compilations use the same requirement and Policy Manifest. The manifest contains `maximum_bytes: 5242880` and the sorted `allowed_media_types` set `image/jpeg`, `image/png`. Every implementation, test, and verification guidance item preserves its source obligation parameters, and template materialization places the exact values in the generated implementation tasks.

The conflicting requirement supplies two unequal maximum byte limits. `ces resolve-policy` writes the diagnostic manifest and returns exit code 4; the manifest records the values deterministically as `conflict_values: [5242880, 10485760]`.

## Automated verification

`corepack pnpm check` passes all typechecks, 99 tests across 19 test files, and every workspace build. The policy-engine tests cover concrete scalar/set resolution and deterministic parameter conflicts. Adapter and implementation-compiler tests cover unchanged propagation through plans, rendered tasks, test manifests, and verification manifests.
