# CES-003 Implementation Evidence

## Resolved artifact

[Profile-picture capability and trait resolution](CES-003-profile-resolution.json)

The artifact contains all three initial capabilities and seven initial traits. Every result includes its deterministic rule ID, evidence path and value, reason, and registry version.

## Automated verification

Command:

```bash
corepack pnpm check
```

Result:

```text
Typecheck: 8 workspace packages passed
Vitest: 7 files passed, 33 tests passed
Build: 8 workspace packages passed
Exit code: 0
```

Resolver tests cover:

- expected positive profile-picture resolution;
- missing-input negative resolution;
- reversed registry rule order;
- evidence deduplication and stable sorting;
- supported assertions kept separate from derived results;
- known but factually unsupported assertions;
- unknown asserted capabilities;
- registry rules targeting unknown IDs.

## Determinism proof

`packages/capability-resolver/src/index.test.ts` reverses every registry rule and compares the complete output object with the default-order result. The objects are equal. The resolver uses code-unit comparisons instead of locale-sensitive ordering and normalizes duplicate evidence by path and value.

## Scope boundary

CES-003 adds no policies and performs no policy selection or adapter behavior. It produces only controlled capabilities, controlled traits, and their traceable evidence.
