# CES-008 Evidence — Laravel reference adapter

## Artifacts

- [Laravel mapping registry](CES-008-mapping-registry.json)
- [Laravel Implementation Package](CES-008-implementation-package.json)
- Shared source Policy Manifest compilation ID: `sha256:240139824b60a83ac65a17fcbf2db171c604f0fd9bc924f3a24402dfe3a545fa`

The cross-adapter portability test passes the same parsed Policy Manifest object to Laravel and the test-fixture adapter. Both output headers retain this compilation ID, and the source object remains byte-unchanged.

## Verification

The Laravel adapter suite covers all nine initial mappings, mapping provenance, PHPUnit guidance, dependency-free `CES-EVIDENCE` markers, missing markers, prohibited client-selected storage paths, explicit gaps, and absence of partial output on exit code 5.

The full workspace check passes 14 test files and 74 tests, along with type checking and production builds. A separate run excluding `adapters/laravel` passes 12 test files and 67 tests, demonstrating that core, CLI, SDK, and fixture behavior do not require the production adapter.

## Dependency boundary

Repository import scans show no `adapters/laravel` or `@company/ces-laravel-adapter` import under `packages/`, `apps/cli`, or the stack-agnostic core. Laravel terminology is confined to adapter source, adapter tests, adapter-derived evidence, and explicitly scoped documentation.
