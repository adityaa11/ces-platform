# CES-016 Evidence — Complete registry content identity

## Manifest identity

The [updated profile-picture Policy Manifest](CES-016-profile-policy-manifest.json) records separate capability, trait, and policy registry hashes. Its compilation ID covers the normalized requirement and assurance input, all three hashes, resolved capabilities and traits, final obligations, and the CES baseline version.

## Mutation matrix

- [Before/after hash matrix](CES-016-hash-matrix.txt)
- [Reproducible matrix generator](CES-016-hash-matrix.mjs)

The matrix holds declared versions constant while independently mutating a capability definition, trait definition, resolver rule, policy definition, policy rule, and parameter binding. Each mutation changes the owning registry hash and final compilation ID.

Policy-engine tests also reverse definitions, rules, dependencies, parameter bindings, and resolver predicates. Equivalent orderings yield byte-identical manifests, while repeat compilation with identical inputs and registries remains byte-identical.

`corepack pnpm check` passes all typechecks, 117 tests across 19 test files, and every workspace build.
