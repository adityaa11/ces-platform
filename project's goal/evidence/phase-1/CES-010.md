# CES-010 Evidence — Initial verification engine

## Persisted reports

- [Passing verification report](CES-010-pass/verification-report.json)
- [Missing-evidence, prohibited-pattern, and failed-test report](CES-010-fail/verification-report.json)
- [Human-review-gated report](CES-010-gated/verification-report.json)
- [CLI exit-code output](CES-010-command-output.txt)

The full workspace check passes 16 test files and 86 tests, along with type checking and production builds for every implementation project.

The passing report references the real files `app/ProfilePictureAction.php` and `tests/Feature/ProfilePictureTest.php`. It validates schemas, source-manifest identity, all nine Laravel evidence markers, a required file, a configured simple pattern, prohibited-pattern absence, and a configured test command.

## Honest semantic boundary

Authorization, atomic replacement, and lifecycle ordering are reported as `human_review_required`; static text discovery is not represented as semantic proof. The default run exits 0 with these findings visible. The assurance-gated fixture sets `gate_human_review` and persists the same findings while exiting 6.

## Deterministic failures

The failing project deliberately omits evidence markers, uses a client-selected storage filename, and runs a configured test command that exits 7. The resulting machine-readable report records each missing marker, the real prohibited-pattern file, command arguments, captured stderr, process exit code, overall failed status, and CES exit code 6.

## Scope

The engine performs bounded schema, identity, file, text-pattern, prohibited-pattern, and configured process checks. Commands are explicit argument arrays executed without a shell. It does not parse an AST or claim automated semantic proof.
