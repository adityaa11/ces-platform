# CES-006 Evidence — Versioned Adapter SDK

## Public API and schemas

The public API is defined in [`packages/adapter-sdk/src/index.ts`](../../../packages/adapter-sdk/src/index.ts). It exports versioned schemas for adapter metadata, policy mappings, provenance-bearing guidance, gap reports, Implementation Packages, Test Manifests, and Verification Manifests. It also exports explicit adapter registration, compatibility checking, and adapter-compilation preflight.

## Verification result

- `corepack pnpm check` completed successfully on 2026-07-22.
- All 11 test files passed, covering 61 tests.
- Adapter SDK contract tests run with no production framework package.
- The suite covers supported mappings, mandatory gaps, provenance enforcement, output versions, manifest and technical compatibility, blocked-manifest rejection, explicit registration, and duplicate registration.
- [Example stack-neutral adapter report](CES-006-adapter-report.json)
- [SDK evolution architecture note](CES-006-architecture.md)

## Gap safety

The preflight result is a discriminated union. Success contains applicable mappings; failure contains only exit code 5 and an `adapter-report.json` value. The gap test asserts that the failure result has no `implementation_package`, preventing incomplete successful artifacts by contract.

## Neutrality and scope scan

The production SDK source contains no production-framework names or defaults. Its only technical compatibility fields are generic language and framework identifiers supplied by adapter metadata. Repository review confirms the package contains no component resolution, dependency solver, generic fallback, downloading, marketplace, or approval implementation.
