# CES-014 Evidence — Controlled facts and trust boundaries

## Schema and CLI boundary

- [Invalid requirement](CES-014-invalid-requirement.yaml)
- [Invalid project](CES-014-invalid-project.yaml)
- [CLI diagnostics and exit codes](CES-014-cli-diagnostics.txt)

The representative `binary-file`, `images`, `own`, and `public` typos are rejected with schema exit code 2. Requirement- and project-schema tests iterate every member of every exported controlled enum and parse it successfully. The Phase 1 guide documents that unknown values are rejected rather than inferred or silently coerced.

## Explicit trust-boundary resolution

The [external-input Policy Manifest](CES-014-external-policy-manifest.json) records `EXTERNAL_INPUT` with evidence path `inputs[0].trust_boundary` and value `external`. Resolver tests also prove that the same binary input with `trust_boundary: internal` does not resolve `EXTERNAL_INPUT`.

`corepack pnpm check` passes all typechecks, 111 tests across 19 test files, and every workspace build.
