# CES-015 Evidence — Project-pinned adapter loading

## Successful exact selection

- [Project-pinned Laravel output](CES-015-project-pinned/implementation-plan.json)
- [Explicit fixture override output](CES-015-fixture-override/implementation-plan.json)

The normal compilation command has no adapter option. Its implementation plan records the exact configured `laravel@0.1.0` pin. The diagnostic fixture override records `test-fixture@0.1.0` and succeeds only with explicit test mode.

## Stable failures

- [Unavailable-version project](CES-015-unavailable-project.yaml)
- [CLI selection diagnostics](CES-015-cli-diagnostics.txt)

An unavailable exact version, a fixture override without test mode, and the removed legacy `--adapter` selector all return schema/selection exit code 2 with stable diagnostics. CLI tests additionally cover configured selection, exact output identity, override parsing, adapter compatibility, and the no-partial-adapter-output behavior.

The [unavailable-version output](CES-015-unavailable/policy-manifest.json) and [rejected fixture output](CES-015-fixture-rejected/policy-manifest.json) contain only the normalized Requirement Package and stack-agnostic Policy Manifest; neither failure emits adapter artifacts.

`corepack pnpm check` passes all typechecks, 115 tests across 19 test files, and every workspace build.
