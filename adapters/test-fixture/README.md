# CES test-fixture adapter

This package is a test-only contract fixture. It is unsafe and unapproved for production guidance and is not the future production `generic-guidance` adapter.

Selection requires explicit test mode. The adapter uses generic pattern names solely to prove Policy Manifest portability, Adapter SDK conformance, deterministic output, and gap behavior without any production framework package.
